Alri
****

* ``GenericTreatment`` at facility level 1a with appointment footprint: ``Over5OPD``.

BladderCancer
*************

* ``Investigation_Following_blood_urine`` at facility level 1b with appointment footprint: ``Over5OPD``.
* ``Investigation_Following_pelvic_pain`` at facility level 1b with appointment footprint: ``Over5OPD``.
* ``MonitorTreatment`` at facility level 3 with appointment footprint: ``Over5OPD``.
* ``PalliativeCare`` at facility level 3 with appointment footprint: ``Over5OPD``.
* ``StartTreatment`` at facility level 3 with appointment footprint: ``Over5OPD``, ``MajorSurg``.

BreastCancer
************

* ``Investigation_Following_breast_lump_discernible`` at facility level 1b with appointment footprint: ``Over5OPD``.
* ``MonitorTreatment`` at facility level 3 with appointment footprint: ``Over5OPD``.
* ``PalliativeCare`` at facility level 3 with appointment footprint: ``Over5OPD``.
* ``StartTreatment`` at facility level 3 with appointment footprint: ``MajorSurg``.

CardioMetabolicDisorders
************************

* ``CommunityTestingForHypertension`` at facility level 1a with appointment footprint: ``Over5OPD``.
* ``InvestigationNotFollowingSymptoms`` at facility level 1b with appointment footprint: ``Over5OPD``.
* ``Investigation_Following_Symptoms`` at facility level 1b with appointment footprint: ``Over5OPD``.
* ``Refill_Medication`` at facility level 1b with appointment footprint: ``Over5OPD``.
* ``SeeksEmergencyCareAndGetsTreatment`` at facility level 2 with appointment footprint: ``Over5OPD``.
* ``StartWeightLossAndMedication`` at facility level 1b with appointment footprint: ``Over5OPD``.

CareOfWomenDuringPregnancy
**************************

* ``AntenatalOutpatientFollowUp`` at facility level 1a with appointment footprint: ``ANCSubsequent``.
* ``AntenatalOutpatientManagementOfAnaemia`` at facility level 1a with appointment footprint: ``ANCSubsequent``.
* ``AntenatalWardInpatientCare`` at facility level ? with appointment footprint: ``InpatientDays`` and bed days footprint: ``general_bed`` (1 days).
* ``EighthAntenatalCareContact`` at facility level ? with appointment footprint: ``ANCSubsequent``.
* ``FifthAntenatalCareContact`` at facility level ? with appointment footprint: ``ANCSubsequent``.
* ``FirstAntenatalCareContact`` at facility level ? with appointment footprint: ``AntenatalFirst``.
* ``FourthAntenatalCareContact`` at facility level ? with appointment footprint: ``ANCSubsequent``.
* ``MaternalEmergencyAssessment`` at facility level 1a with appointment footprint: ``Over5OPD``.
* ``PostAbortionCaseManagement`` at facility level 1a with appointment footprint: ``InpatientDays`` and bed days footprint: ``general_bed`` (3 days).
* ``PresentsForInductionOfLabour`` at facility level 1a with appointment footprint: ``Over5OPD``.
* ``SecondAntenatalCareContact`` at facility level ? with appointment footprint: ``ANCSubsequent``.
* ``SeventhAntenatalCareContact`` at facility level ? with appointment footprint: ``ANCSubsequent``.
* ``SixthAntenatalCareContact`` at facility level ? with appointment footprint: ``ANCSubsequent``.
* ``ThirdAntenatalCareContact`` at facility level ? with appointment footprint: ``ANCSubsequent``.
* ``TreatmentForEctopicPregnancy`` at facility level 1b with appointment footprint: ``MajorSurg`` and bed days footprint: ``general_bed`` (5 days).

Contraception
*************

* ``FamilyPlanningAppt`` at facility level 1a with appointment footprint: ``FamPlan``.

Depression
**********

* ``Antidepressant_Refill`` at facility level 1a with appointment footprint: ``Over5OPD``.
* ``Antidepressant_Start`` at facility level 1a with appointment footprint: ``Over5OPD``.
* ``TalkingTherapy`` at facility level 1a with appointment footprint: ``Over5OPD``.

Diarrhoea
*********

* ``Treatment_Inpatient`` at facility level 1a with appointment footprint: ``InpatientDays``, ``IPAdmission`` and bed days footprint: ``general_bed`` (2 days).
* ``Treatment_Outpatient`` at facility level 1a with appointment footprint: ``Under5OPD``.

Epi
***

* ``DtpHibHep`` at facility level 0 with appointment footprint: ``ConWithDCSA``.
* ``MeaslesRubella`` at facility level 0 with appointment footprint: ``ConWithDCSA``.
* ``Pneumo`` at facility level 0 with appointment footprint: ``ConWithDCSA``.
* ``Rota`` at facility level 0 with appointment footprint: ``ConWithDCSA``.
* ``Td`` at facility level 0 with appointment footprint: ``ConWithDCSA``.
* ``bcg`` at facility level 0 with appointment footprint: ``ConWithDCSA``.
* ``hpv`` at facility level 0 with appointment footprint: ``ConWithDCSA``.
* ``opv`` at facility level 0 with appointment footprint: ``ConWithDCSA``.

Epilepsy
********

* ``Start_Anti-Epilpetics`` at facility level 1a with appointment footprint: ``Over5OPD``.

HealthSeekingBehaviour
**********************

* ``GenericEmergencyFirstApptAtFacilityLevel1`` at facility level 1b with appointment footprint: ``Over5OPD``.
* ``GenericFirstApptAtFacilityLevel0`` at facility level 0 with appointment footprint: ``ConWithDCSA``.

Hiv
***

* ``Circumcision`` at facility level 1a with appointment footprint: ``MaleCirc``.
* ``StartOrContinueOnPrep`` at facility level 1a with appointment footprint: ``Over5OPD``.
* ``TestAndRefer`` at facility level 1a with appointment footprint: ``VCTNegative``.
* ``Treatment_InitiationOrContinuation`` at facility level 1a with appointment footprint: ``Over5OPD``, ``NewAdult``.

Labour
******

* ``ReceivesCareFollowingCaesareanSection`` at facility level ? with appointment footprint: ``InpatientDays`` and bed days footprint: ``general_bed`` (2 days).
* ``ReceivesComprehensiveEmergencyObstetricCare`` at facility level 1b with appointment footprint: ``MajorSurg`` and bed days footprint: ``general_bed`` (2 days).
* ``ReceivesSkilledBirthAttendanceDuringLabour`` at facility level ? with appointment footprint: ``NormalDelivery`` and bed days footprint: ``general_bed`` (1 days).
* ``ReceivesSkilledBirthAttendanceFollowingLabour`` at facility level ? with appointment footprint: ``InpatientDays`` and bed days footprint: ``general_bed`` (2 days).

Malaria
*******

* ``IPTp`` at facility level 1a with appointment footprint: ``AntenatalFirst``.
* ``RDT`` at facility level 0 with appointment footprint: ``ConWithDCSA``.
* ``treatment_adult`` at facility level 1a with appointment footprint: ``Over5OPD``.
* ``treatment_child0_5`` at facility level 0 with appointment footprint: ``ConWithDCSA``.
* ``treatment_child5_15`` at facility level 1a with appointment footprint: ``Under5OPD``.
* ``treatment_complicated_adult`` at facility level 1b with appointment footprint: ``InpatientDays``.
* ``treatment_complicated_child`` at facility level 1b with appointment footprint: ``InpatientDays``.

Measles
*******

* ``Treatment`` at facility level 1a with appointment footprint: ``Over5OPD``.

NewbornOutcomes
***************

* ``CareOfTheNewbornBySkilledAttendant`` at facility level ? with appointment footprint: ``InpatientDays`` and bed days footprint: ``general_bed`` (2 days).

OesophagealCancer
*****************

* ``Investigation_Following_Dysphagia`` at facility level 1b with appointment footprint: ``Over5OPD``.
* ``MonitorTreatment`` at facility level 2 with appointment footprint: ``Over5OPD``.
* ``PalliativeCare`` at facility level 2 with appointment footprint: ``Over5OPD``.
* ``StartTreatment`` at facility level 3 with appointment footprint: ``Over5OPD``, ``MajorSurg``.

OtherAdultCancer
****************

* ``Investigation_Following_other_adult_ca_symptom`` at facility level 1b with appointment footprint: ``Over5OPD``.
* ``MonitorTreatment`` at facility level 3 with appointment footprint: ``Over5OPD``.
* ``PalliativeCare`` at facility level 3 with appointment footprint: ``Over5OPD``.
* ``StartTreatment`` at facility level 3 with appointment footprint: ``MajorSurg``.

PostnatalSupervisor
*******************

* ``NeonatalWardInpatientCare`` at facility level 1a with appointment footprint: ``IPAdmission`` and bed days footprint: ``general_bed`` (5 days).
* ``PostnatalCareContactOne`` at facility level 1a with appointment footprint: ``ANCSubsequent``.
* ``PostnatalCareContactTwoMaternal`` at facility level 1a with appointment footprint: ``ANCSubsequent``.
* ``PostnatalWardInpatientCare`` at facility level 1a with appointment footprint: ``IPAdmission`` and bed days footprint: ``general_bed`` (5 days).
* ``TreatmentForObstetricFistula`` at facility level 1a with appointment footprint: ``InpatientDays`` and bed days footprint: ``general_bed`` (5 days).

ProstateCancer
**************

* ``Investigation_Following_blood_urine`` at facility level 1b with appointment footprint: ``Over5OPD``.
* ``Investigation_Following_pelvic_pain`` at facility level 1b with appointment footprint: ``Over5OPD``.
* ``Investigation_Following_psa_positive`` at facility level 1b with appointment footprint: ``Over5OPD``.
* ``MonitorTreatment`` at facility level 3 with appointment footprint: ``Over5OPD``.
* ``PalliativeCare`` at facility level 3 with appointment footprint: ``Over5OPD``.
* ``StartTreatment`` at facility level 3 with appointment footprint: ``MajorSurg``.

RTI
***

* ``Acute_Pain_Management`` at facility level 1b with appointment footprint: ``Over5OPD``.
* ``Burn_Management`` at facility level 1b with appointment footprint: ``MinorSurg``.
* ``Fracture_Cast`` at facility level 1b with appointment footprint: ``AccidentsandEmerg``.
* ``Imaging_Event`` at facility level 1b with appointment footprint: .
* ``Major_Surgeries`` at facility level 1b with appointment footprint: ``MajorSurg``.
* ``MedicalIntervention`` at facility level 1b with appointment footprint: ``AccidentsandEmerg``, ``InpatientDays`` and bed days footprint: ``general_bed`` (8 days).
* ``Minor_Surgeries`` at facility level 1b with appointment footprint: ``MinorSurg``.
* ``Open_Fracture_Treatment`` at facility level 1b with appointment footprint: ``MinorSurg``.
* ``Shock_Treatment`` at facility level 1b with appointment footprint: ``AccidentsandEmerg``.
* ``Suture`` at facility level 1b with appointment footprint: ``Over5OPD``.
* ``Tetanus_Vaccine`` at facility level 1b with appointment footprint: ``Over5OPD``.

Stunting
********

* ``Complementary_feeding_for_stunting`` at facility level 1a with appointment footprint: ``Under5OPD``.

